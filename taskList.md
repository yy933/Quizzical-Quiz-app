## 1. Project Setup
- Initialize project：Initialize React + JavaScript project with Vite 
- Remove unnecessary default CSS and `.js/.jsx` files
- Basic Styling：fonts, background, background color, etc 
- Install packages： `he` or `html-entities`（Decode HTML entities in data retrieving from Open Trivia API）, `nanoid`。